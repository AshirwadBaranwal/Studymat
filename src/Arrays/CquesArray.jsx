const CquesArray = [
  {
    id: 0,
    Question:
      " Write a program in C to take input of principal , rate and time and then calculate Simple interest.",
  },
];

export default CquesArray;
export const CquesArrayQuestions = CquesArray.map((p) => p.Question);
